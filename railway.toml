[build]
dockerfilePath = "Dockerfile.api"

[deploy]
startCommand = "/bin/sh -c \"uvicorn api.main:app --host 0.0.0.0 --port $PORT\""
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
