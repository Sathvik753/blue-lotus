// Feature flags. Flip PAYMENTS_ENABLED back to true once Stripe is wired up —
// while false, paid plan buttons show a "temporarily disabled" message instead
// of routing to checkout/registration. (Sandbox + Institutional stay active.)
export const PAYMENTS_ENABLED = false;
